# Triggers
# This pipeline will be trigger manually for releases
trigger: none

jobs:
  - template: '.azure/templates/system_test_template.yaml'
    parameters:
      name: 'helm-acceptance'
      test_case: '.*ST'
      groups: 'acceptance'
      cluster_operator_install_type: 'helm'


